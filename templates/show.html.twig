{% extends 'base.html.twig' %}
{% block body %}
    {% if slug %}
        <h1>{{ slug }}</h1>
    {% else %}
        <h1>Aucune série sélectionnée, veuillez choisir une série</h1>
    {% endif %}
{% endblock %}